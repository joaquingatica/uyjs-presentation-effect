---
layout: intro
---

# Overview de Effect

---

# ¿Quién alguna vez...?

<v-clicks>

- Recibió un code review con "No te olvides del **error handling**"
- Tuvo que **convertir a async** en 20 lugares solo por una función que cambió la firma
- Tuvo que lidiar con **logs eternos no estandarizados**
- Elegir entre **miles de librerías** distintas para levantar un express server
- Tuvo que parsear y validar muchas **environment variables** a mano
- Quizo **reutilizar código entre front y back** y se complicó
- Tuvo que acordarse de aplicar las **mejores prácticas** más básicas

</v-clicks>

---
layout: fact
---

Entonces...

<v-clicks>

## Effect puede ser para vos ✅

Para **convertir en expresiones** lo que suelen ser convenciones

Para **hacer visible** lo no evidente

Para que el **framework me obligue** en lugar de acordarme

</v-clicks>

---
layout: two-cols-header
---

<<< @/snippets/overview-sample-fn-left.ts#snippet {all}

<br />

::left::

<v-click>

#### ¿Qué sabemos?

</v-click>

<v-clicks>

- Que es asíncrona
- Que espera un ID de usuario
- Que manda email si logra obtener usuario y template

</v-clicks>

::right::

<v-click>

#### ¿Qué no sabemos?

</v-click>

<v-clicks>

- ¿Hace HTTP requests?
- ¿En qué casos puede fallar?
- ¿Con qué errores falla?
- ¿Usa la base de datos?
- ¿Necesita algún proveedor de email?

</v-clicks>

---
layout: intro
---

## ¿Cómo mejorar esto?

Usando el concepto de un **efecto** que **declare** lo que va a suceder.

<v-click>

Representado como:

```ts
//            ┌─── Tipo del resultado
//            │        ┌─── Tipo del error
//            │        │      ┌─── Las dependencias
//            ▼        ▼      ▼
type Effect<Success, Error, Requirements>
```

</v-click>

<v-clicks>

- Es el tipo base del framework
- Cada effect es una unidad de cómputo autónoma
- Una variable de tipo `Effect` describe y contiene lo que se va a ejecutar
- El framework provee APIs y librerías para componer Effects

</v-clicks>

---
layout: two-cols-header
---

## Creando Effects

Declarando un comportamiento sin ejecutarlo aún.

```ts
type Effect<Success, Error, Requirements>
```

<br />

::left::

```ts {1,2|4-5|7-11|13-17}
// Effect<User, never, never>
const program = Effect.succeed({ id: 1, name: 'John' })

// Effect<never, ParseError, never>
const program = Effect.fail(new ParseError('Invalid'))

// Effect<User, ParseError | HttpError, HttpClient>
const program = Effect.gen(function* () {
  const data = yield* fetchUsers
  return yield* parseUserFromData(data)
})

// Effect<User[], DbError, DbService>
const program = Effect.gen(function* () {
  const { getUsers } = yield* DbService
  return yield* getUsers()
})
```

::right::

<v-click>

Utiliza generators para **componer y establecer
dependencias** entre effects.

`yield*` "pide" resultado de otro `Effect`.

Similar al `await` de una `Promise`.

</v-click>

---
layout: default
---

## Ejecutando Effects

Un `Effect` describe, el framework lo ejecuta. ¿Cómo?

<v-click>

Agnóstico a la plataforma:

```ts {3-4|6-7}
const program = Effect.succeed(1)

// result: number
const result = Effect.runSync(program)

// result: Promise<number>
const result = Effect.runPromise(program)
```

</v-click>

<v-click>

Específico a la plataforma:

```ts {1|5}
import { NodeContext, NodeRuntime } from '@effect/platform-node'

const program = Effect.succeed(1)

NodeRuntime.runMain(program.pipe(Effect.provide(NodeContext.layer)))
```

</v-click>

---
layout: two-cols-header
---

## Aplicando Effect a lo anterior...

<br />

::left::

<<< @/snippets/overview-sample-fn-left.ts#snippet {all|1,3,6,8}

<br />

<v-click>

#### Ya sabíamos que...

- Que es asíncrona
- Que espera un ID de usuario
- Que manda email si logra obtener usuario y template

</v-click>

::right::

<<< @/snippets/overview-sample-fn-right.ts#snippet {hide|all|1,3,4,7,8}

<br />

<v-click>

#### ¿Qué nos agrega?

</v-click>

<v-clicks>

- Usa la base de datos de `DbService`
- Puede fallar por `UserError` o `EmailError`
- Tiene un proveedor de email en `EmailService`

</v-clicks>

---
layout: intro
---

## Hablemos de Programación Funcional...

---

Desmitificando el `pipe`:

- menos carga cognitiva de nombrar
- menos carga cognitiva de lectura
- facilidad de inyectar comportamiento
- fomenta funciones puras (Effect ya lo hace)
- builder pattern on steroids

Ejemplo de las string operations

---
layout: two-cols-header
---

## Y si quieren profundizar...

Effect nació como framework funcional inspirado en `fp-ts` y ZIO de Scala.

<br />

::left::

<<< @/snippets/overview-sample-fn-right-fp.ts#snippet {hide|all|1,2|3,14|3,4,14|3,5,14|3,6,14|3,7,8,14|3,9,14|3,10,11,14|3,12,13,14|all}

::right::

<v-click>

La **mónada** del framework es `Effect`

Componer efectos con `pipe`.

Operadores `andThen`, `orElse`, `catchTags`

</v-click>
